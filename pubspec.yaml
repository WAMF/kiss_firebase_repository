name: kiss_firebase_repository
description: "A firebase implementation of kiss_repository interface"
version: 0.9.0
homepage: https://github.com/WAMF/kiss_firebase_repository

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^5.6.8
  firebase_core: ^3.13.1
  
  kiss_repository: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  kiss_repository_tests: ^0.2.0
  very_good_analysis: ^6.0.0


